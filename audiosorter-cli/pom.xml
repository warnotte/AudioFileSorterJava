<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.github.warnotte</groupId>
        <artifactId>audiosorter-parent</artifactId>
        <version>0.2.0</version>
    </parent>

    <artifactId>audiosorter-cli</artifactId>
    <packaging>jar</packaging>

    <name>AudioSorter CLI</name>
    <description>Command-line interface for AudioSorter</description>

    <properties>
        <app.name>AudioFilesSorter</app.name>
        <app.version>${project.version}</app.version>
    </properties>

    <dependencies>
        <!-- Core module -->
        <dependency>
            <groupId>io.github.warnotte</groupId>
            <artifactId>audiosorter-core</artifactId>
        </dependency>

        <!-- CLI parsing -->
        <dependency>
            <groupId>info.picocli</groupId>
            <artifactId>picocli</artifactId>
        </dependency>

        <!-- Logging -->
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-core</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- Create executable fat JAR -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>io.github.warnotte.audiosorter.cli.Main</mainClass>
                                </transformer>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
                            </transformers>
                            <filters>
                                <filter>
                                    <artifact>*:*</artifact>
                                    <excludes>
                                        <exclude>META-INF/*.SF</exclude>
                                        <exclude>META-INF/*.DSA</exclude>
                                        <exclude>META-INF/*.RSA</exclude>
                                    </excludes>
                                </filter>
                            </filters>
                            <finalName>AudioFilesSorter-${project.version}</finalName>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!-- Profile for creating distributable app-image with minimal JRE -->
        <profile>
            <id>dist</id>
            <properties>
                <!-- JDK modules required by the application (determined via jdeps) -->
                <jlink.modules>java.base,java.compiler,java.desktop,java.management,java.naming,java.rmi,java.scripting,java.sql</jlink.modules>
            </properties>
            <build>
                <plugins>
                    <!-- Step 1: Create minimal custom JRE with jlink -->
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-antrun-plugin</artifactId>
                        <version>3.1.0</version>
                        <executions>
                            <execution>
                                <id>create-custom-jre</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>run</goal>
                                </goals>
                                <configuration>
                                    <target>
                                        <!-- Delete existing dirs to allow rebuild -->
                                        <delete dir="${project.build.directory}/custom-jre" quiet="true"/>
                                        <delete dir="${project.build.directory}/dist" quiet="true"/>
                                        <delete dir="${project.build.directory}/jpackage-input" quiet="true"/>
                                        <!-- Run jlink to create minimal JRE -->
                                        <exec executable="jlink" failonerror="true">
                                            <arg value="--add-modules"/>
                                            <arg value="${jlink.modules}"/>
                                            <arg value="--strip-debug"/>
                                            <arg value="--no-man-pages"/>
                                            <arg value="--no-header-files"/>
                                            <arg value="--compress=zip-6"/>
                                            <arg value="--output"/>
                                            <arg value="${project.build.directory}/custom-jre"/>
                                        </exec>
                                        <!-- Create input dir with only the fat JAR -->
                                        <mkdir dir="${project.build.directory}/jpackage-input"/>
                                        <copy file="${project.build.directory}/AudioFilesSorter-${project.version}.jar"
                                              todir="${project.build.directory}/jpackage-input"/>
                                    </target>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>

                    <!-- Step 2: jpackage using the custom minimal JRE -->
                    <plugin>
                        <groupId>org.panteleyev</groupId>
                        <artifactId>jpackage-maven-plugin</artifactId>
                        <version>1.6.5</version>
                        <executions>
                            <execution>
                                <id>create-app-image</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>jpackage</goal>
                                </goals>
                                <configuration>
                                    <type>APP_IMAGE</type>
                                    <name>${app.name}</name>
                                    <appVersion>${app.version}</appVersion>
                                    <vendor>warnotte</vendor>
                                    <description>Audio file sorting and cataloging tool</description>
                                    <input>${project.build.directory}/jpackage-input</input>
                                    <mainJar>AudioFilesSorter-${project.version}.jar</mainJar>
                                    <mainClass>io.github.warnotte.audiosorter.cli.Main</mainClass>
                                    <destination>${project.build.directory}/dist</destination>
                                    <!-- Use custom minimal JRE (47 MB) instead of full JDK (152 MB) -->
                                    <runtimeImage>${project.build.directory}/custom-jre</runtimeImage>
                                    <javaOptions>
                                        <option>-Xmx512m</option>
                                    </javaOptions>
                                    <!-- Windows specific -->
                                    <winConsole>true</winConsole>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>

                    <!-- Create ZIP of the app-image -->
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-assembly-plugin</artifactId>
                        <version>3.6.0</version>
                        <executions>
                            <execution>
                                <id>create-distribution-zip</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>single</goal>
                                </goals>
                                <configuration>
                                    <descriptors>
                                        <descriptor>src/assembly/dist.xml</descriptor>
                                    </descriptors>
                                    <finalName>${app.name}-${app.version}-windows</finalName>
                                    <appendAssemblyId>false</appendAssemblyId>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
